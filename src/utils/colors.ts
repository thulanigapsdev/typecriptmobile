const backdrop = '#22d3ee';
const background = '#164e63';

const error = '#ef4444';
const success = '#06b6d4';

const disabled_bg = '#64748b';
const disabled_text = '#1f2937';

const default_btn_bg = '#0e7490';

const white = '#ffffff';
const black = '#121212';

const colors = {
  backdrop,
  background,
  error,
  disabled_bg,
  disabled_text,
  white,
  black,
  default_btn_bg,
  success,
};

export default colors;
